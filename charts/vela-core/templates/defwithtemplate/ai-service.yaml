# Code generated by KubeVela templates. DO NOT EDIT. Please edit the original cue file.
# Definition source cue file: vela-templates/definitions/internal/component/ai-service.cue
apiVersion: core.oam.dev/v1beta1
kind: ComponentDefinition
metadata:
  annotations:
    definition.oam.dev/description: Describes long-running AI inference or model serving workloads while preserving the native KubeVela Application model.
  name: ai-service
  namespace: {{ include "systemDefinitionNamespace" . }}
spec:
  schematic:
    cue:
      template: |2

        let aiManagementLabels = {
        	"app.oam.dev/name":         context.appName
        	"app.oam.dev/component":    context.name
        	"ai.oam.dev/workload-kind": "service"
        	"ai.oam.dev/model":         parameter.model.name
        }

        output: {
        	apiVersion: "apps/v1"
        	kind:       "Deployment"
        	metadata: labels: aiManagementLabels
        	spec: {
        		replicas: parameter.replicas
        		if parameter.strategy != _|_ {
        			strategy: parameter.strategy
        		}
        		if parameter.minReadySeconds != _|_ {
        			minReadySeconds: parameter.minReadySeconds
        		}
        		if parameter.progressDeadlineSeconds != _|_ {
        			progressDeadlineSeconds: parameter.progressDeadlineSeconds
        		}
        		if parameter.revisionHistoryLimit != _|_ {
        			revisionHistoryLimit: parameter.revisionHistoryLimit
        		}
        		selector: matchLabels: "app.oam.dev/component": context.name
        		template: {
        			metadata: {
        				labels: {
        					if parameter.labels != _|_ {
        						parameter.labels
        					}
        					aiManagementLabels
        				}
        				annotations: {
        					"ai.oam.dev/workload-kind": "service"
        					"ai.oam.dev/model-name":    parameter.model.name
        					if parameter.model.version != _|_ {
        						"ai.oam.dev/model-version": parameter.model.version
        					}
        					if parameter.annotations != _|_ {
        						parameter.annotations
        					}
        				}
        			}
        			spec: {
        				if parameter.serviceAccountName != _|_ {
        					serviceAccountName: parameter.serviceAccountName
        				}
        				if parameter.automountServiceAccountToken != _|_ {
        					automountServiceAccountToken: parameter.automountServiceAccountToken
        				}
        				if parameter.imagePullSecrets != _|_ {
        					imagePullSecrets: parameter.imagePullSecrets
        				}
        				if parameter.nodeSelector != _|_ {
        					nodeSelector: parameter.nodeSelector
        				}
        				if parameter.priorityClassName != _|_ {
        					priorityClassName: parameter.priorityClassName
        				}
        				if parameter.runtimeClassName != _|_ {
        					runtimeClassName: parameter.runtimeClassName
        				}
        				if parameter.schedulerName != _|_ {
        					schedulerName: parameter.schedulerName
        				}
        				if parameter.securityContext != _|_ {
        					securityContext: parameter.securityContext
        				}
        				if parameter.terminationGracePeriodSeconds != _|_ {
        					terminationGracePeriodSeconds: parameter.terminationGracePeriodSeconds
        				}
        				if parameter.volumes != _|_ {
        					volumes: parameter.volumes
        				}
        				containers: [{
        					name:  context.name
        					image: parameter.image
        					if parameter["imagePullPolicy"] != _|_ {
        						imagePullPolicy: parameter.imagePullPolicy
        					}
        					if parameter.containerSecurityContext != _|_ {
        						securityContext: parameter.containerSecurityContext
        					}
        					if parameter["cmd"] != _|_ {
        						command: parameter.cmd
        					}
        					if parameter["args"] != _|_ {
        						args: parameter.args
        					}
        					env: [
        						{
        							name:  "MODEL_NAME"
        							value: parameter.model.name
        						},
        						if parameter.model.version != _|_ {
        							name:  "MODEL_VERSION"
        							value: parameter.model.version
        						},
        						if parameter.model.uri != _|_ {
        							name:  "MODEL_URI"
        							value: parameter.model.uri
        						},
        						if parameter["env"] != _|_ for e in parameter.env {e},
        					]
        					if parameter.envFrom != _|_ {
        						envFrom: parameter.envFrom
        					}
        					if parameter.endpoint.port != _|_ {
        						ports: [{
        							name:          parameter.endpoint.name
        							containerPort: parameter.endpoint.port
        						}]
        					}
        					if parameter.resources != _|_ {
        						resources: parameter.resources
        					}
        					if parameter.lifecycle != _|_ {
        						lifecycle: parameter.lifecycle
        					}
        					if parameter.volumeMounts != _|_ {
        						volumeMounts: parameter.volumeMounts
        					}
        					if parameter.readinessProbe != _|_ {
        						readinessProbe: parameter.readinessProbe
        					}
        					if parameter.livenessProbe != _|_ {
        						livenessProbe: parameter.livenessProbe
        					}
        					if parameter.startupProbe != _|_ {
        						startupProbe: parameter.startupProbe
        					}
        				}]
        			}
        		}
        	}
        }
        if parameter.endpoint.port != _|_ {
        	outputs: service: {
        		apiVersion: "v1"
        		kind:       "Service"
        		metadata: {
        			name: "\(context.appName)-\(context.name)"
        			labels: {
        				aiManagementLabels
        				if parameter.endpoint.labels != _|_ {
        					parameter.endpoint.labels
        				}
        			}
        			if parameter.endpoint.annotations != _|_ {
        				annotations: parameter.endpoint.annotations
        			}
        		}
        		spec: {
        			type: parameter.endpoint.type
        			selector: "app.oam.dev/component": context.name
        			ports: [{
        				name:       parameter.endpoint.name
        				port:       parameter.endpoint.servicePort
        				targetPort: parameter.endpoint.port
        			}]
        		}
        	}
        }

        parameter: {
        	// +usage=Container image for the AI service runtime
        	image: string
        	// +usage=Replica count for the model serving deployment
        	replicas: *1 | int & >=1
        	// +usage=Kubernetes Deployment update strategy for the AI service
        	strategy?: {...}
        	// +usage=Minimum seconds a new AI service pod should be ready before being treated as available
        	minReadySeconds?: int & >=0
        	// +usage=Maximum seconds for the AI service Deployment rollout to make progress
        	progressDeadlineSeconds?: int & >=1
        	// +usage=Number of old ReplicaSets retained by the AI service Deployment
        	revisionHistoryLimit?: int & >=0
        	// +usage=Container image pull policy
        	imagePullPolicy?: "Always" | "Never" | "IfNotPresent"
        	// +usage=Service account used by the AI service pods
        	serviceAccountName?: string
        	// +usage=Whether to automount the service account token into AI service pods
        	automountServiceAccountToken?: bool
        	// +usage=Image pull secrets used by the AI service pods
        	imagePullSecrets?: [...{
        		name: string
        	}]
        	// +usage=Node labels required by the AI service pods
        	nodeSelector?: [string]: string
        	// +usage=Priority class name used by the AI service pods
        	priorityClassName?: string
        	// +usage=Runtime class name used by the AI service pods
        	runtimeClassName?: string
        	// +usage=Scheduler name used by the AI service pods
        	schedulerName?: string
        	// +usage=Kubernetes pod security context used by the AI service pods
        	securityContext?: {...}
        	// +usage=Kubernetes container security context used by the AI service container
        	containerSecurityContext?: {...}
        	// +usage=Grace period in seconds before forcibly terminating AI service pods
        	terminationGracePeriodSeconds?: int & >=0
        	// +usage=Model metadata consumed by the serving runtime and platform observability
        	model: {
        		// +usage=Logical model name
        		name: string
        		// +usage=Model version or revision
        		version?: string
        		// +usage=Model artifact URI
        		uri?: string
        	}
        	// +usage=Network endpoint exposed by the model serving container
        	endpoint: {
        		name:        *"http" | string
        		port?:       int
        		servicePort: *80 | int
        		type:        *"ClusterIP" | "ClusterIP" | "NodePort" | "LoadBalancer"
        		// +usage=Labels on the generated Kubernetes Service
        		labels?: [string]: string
        		// +usage=Annotations on the generated Kubernetes Service
        		annotations?: [string]: string
        	}
        	// +usage=Commands to run in the container
        	cmd?: [...string]
        	// +usage=Arguments to run in the container
        	args?: [...string]
        	// +usage=Environment variables for the container
        	env?: [...{
        		name:   string
        		value?: string
        		valueFrom?: {...}
        	}]
        	// +usage=Environment variable sources for the container
        	envFrom?: [...{...}]
        	// +usage=Labels on the workload pod template
        	labels?: [string]: string
        	// +usage=Annotations on the workload pod template
        	annotations?: [string]: string
        	// +usage=Kubernetes resource requests and limits
        	resources?: {
        		requests?: [string]: string
        		limits?: [string]:   string
        	}
        	// +usage=Kubernetes volumes attached to the AI service pod
        	volumes?: [...{...}]
        	// +usage=Kubernetes lifecycle hooks attached to the AI service container
        	lifecycle?: {...}
        	// +usage=Kubernetes volume mounts attached to the AI service container
        	volumeMounts?: [...{...}]
        	// +usage=Kubernetes readiness probe for the AI service container
        	readinessProbe?: {...}
        	// +usage=Kubernetes liveness probe for the AI service container
        	livenessProbe?: {...}
        	// +usage=Kubernetes startup probe for the AI service container
        	startupProbe?: {...}
        }
  status:
    customStatus: |-
      ready: {
      	readyReplicas: *0 | int
      } & {
      	if context.output.status.readyReplicas != _|_ {
      		readyReplicas: context.output.status.readyReplicas
      	}
      }
      message: "Ready:\(ready.readyReplicas)/\(context.output.spec.replicas)"
    healthPolicy: |-
      ready: {
      	updatedReplicas:    *0 | int
      	readyReplicas:      *0 | int
      	replicas:           *0 | int
      	observedGeneration: *0 | int
      } & {
      	if context.output.status.updatedReplicas != _|_ {
      		updatedReplicas: context.output.status.updatedReplicas
      	}
      	if context.output.status.readyReplicas != _|_ {
      		readyReplicas: context.output.status.readyReplicas
      	}
      	if context.output.status.replicas != _|_ {
      		replicas: context.output.status.replicas
      	}
      	if context.output.status.observedGeneration != _|_ {
      		observedGeneration: context.output.status.observedGeneration
      	}
      }
      isHealth: (context.output.spec.replicas == ready.readyReplicas) && (context.output.spec.replicas == ready.updatedReplicas) && (context.output.spec.replicas == ready.replicas) && (ready.observedGeneration == context.output.metadata.generation || ready.observedGeneration > context.output.metadata.generation)
  workload:
    definition:
      apiVersion: apps/v1
      kind: Deployment
    type: deployments.apps

